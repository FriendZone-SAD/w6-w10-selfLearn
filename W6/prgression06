ผมใช้ chatgpt ในการ gen โจทย์มาให้แล้วผมก็ทำตามโจทย์ที่ให้มาโดยผมบอกเนื้อหาที่เรียนมา

---

# 📝 Self Practice Tasks (Functional Programming – JavaScript)

### 1. First-Class & Higher-Order Functions

**Task:**
เขียนฟังก์ชัน `applyTwice(func, value)` ที่รับฟังก์ชันกับค่าเข้ามา แล้วนำฟังก์ชันนั้นไปใช้กับค่า **2 รอบ**

```js
// Example
const double = x => x * 2
console.log(applyTwice(double, 5)) // 20
```

---

### 2. Declarative Code

**Task:**
ให้ array ตัวเลข `[3, 7, 10, 15, 20, 25]`

* ใช้ **filter** หาเลขที่หารด้วย 5 ลงตัว
* ใช้ **map** ยกกำลังสองทุกตัวเลขที่เหลือ
* แสดงผลลัพธ์

---

### 3. Immutability

**Task:**
เขียนโค้ดที่

* มี array `[1, 2, 3, 4]`
* สร้าง array ใหม่ที่บวกเลขแต่ละตัวด้วย 10 (โดยไม่แก้ไข array เก่า)
* แสดงทั้ง array เก่าและ array ใหม่ออกมา

---

### 4. Pure vs Impure Functions

**Task:**

* เขียนฟังก์ชัน **pure function** ที่รับตัวเลขมาแล้วบวก 100
* เขียนฟังก์ชัน **impure function** ที่ทำให้ค่าของตัวแปร global `count` เพิ่มขึ้นทุกครั้งที่เรียก

---

### 5. Function Composition

**Task:**
เขียนฟังก์ชัน `compose(f, g)` ที่รับฟังก์ชัน 2 ตัวแล้ว return ฟังก์ชันใหม่ที่เป็นการเอา `f(g(x))`

```js
// Example
const addOne = x => x + 1
const double = x => x * 2
const addOneThenDouble = compose(double, addOne)

console.log(addOneThenDouble(5)) // 12
```

---

### 6. Function Types

**Task:**
เขียนฟังก์ชัน 3 แบบ

1. **Named function** → `function add(a, b)`
2. **Anonymous function (expression)** → `const sub = function(a, b)`
3. **Arrow function** → `const mul = (a, b) =>`

---

### 7. Function Scope & Nested Functions

**Task:**
เขียนฟังก์ชัน `calculator(a, b)` ที่มีฟังก์ชันย่อย 2 ตัว `add()` และ `multiply()` อยู่ข้างใน แล้ว return ผลลัพธ์ของการบวกและคูณ

---

### 8. Closures

**Task:**
เขียนฟังก์ชัน `makeCounter()` ที่ return ฟังก์ชัน 2 ตัวคือ `increment()` และ `getValue()` โดยใช้ closure เก็บค่า count เอาไว้

```js
const c = makeCounter()
c.increment()  
c.increment()  
console.log(c.getValue()) // 2
```